# 画面設計書 - カレンダー共有アプリ

## 1. 画面一覧

| #   | 画面名                 | パス                  | 認証 | グループ             |
| --- | ---------------------- | --------------------- | ---- | -------------------- |
| 1   | ログイン               | `/login`              | 不要 | 不問                 |
| 2   | 新規登録               | `/register`           | 不要 | 不問                 |
| 3   | オンボーディング       | `/onboarding`         | 必要 | 未所属のみ           |
| 4   | グループ参加           | `/join/[token]`       | 必要 | 1グループ以上所属可  |
| 5   | プライベートカレンダー | `/`                   | 必要 | 所属済み             |
| 5.5 | TODOリスト           | `/todo`               | 必要 | 所属済み             |
| 6   | イベント作成           | `/events/new`         | 必要 | 所属済み             |
| 7   | イベント編集           | `/events/[id]/edit`   | 必要 | 所属済み・作成者のみ |
| 8   | 仕事用カレンダー       | `/work`               | 必要 | 所属済み             |
| 9   | シフトパターン一覧     | `/work/patterns`      | 必要 | 所属済み             |
| 10  | シフトパターン新規作成 | `/work/patterns/new`  | 必要 | 所属済み             |
| 11  | シフトパターン編集     | `/work/patterns/[id]` | 必要 | 所属済み・本人のみ   |
| 12  | グループ設定           | `/groups-settings`           | 必要 | 不問                 |
| 13  | グループ作成           | `/groups/new`         | 必要 | 不問                 |
| 14  | プロフィール設定       | `/groups-settings/profile`   | 必要 | 不問                 |
| 15  | アカウント設定         | `/groups-settings/account`   | 必要 | 不問                 |
| 16  | カラー編集             | `/color-labels`       | 必要 | 不問                 |

---

## 2. 画面遷移図

```
┌─────────────────────────────────────────────────────────────┐
│ 未認証ルート                                                  │
│                                                             │
│  /login ──────────────────────────────── /register          │
│    │  ↑ ログイン失敗                          │              │
│    │  └─────────────────────────────────────┘              │
│    │ ログイン成功                                            │
└────┼────────────────────────────────────────────────────────┘
     │
     ├─ グループ未所属 ──▶ /onboarding
     │                       │
     │                       ├─ グループ作成 ──▶ /
     │                       └─ 招待URLをコピー（メンバーへ共有）
     │
     │  ※招待リンク経由（別途アクセス）
     │  /join/[token] ──▶ グループ参加 ──▶ /
     │
     └─ グループ所属済み
           │
           ├─▶ /（プライベートカレンダー）
           │       │
           │       ├─ グループ名タップ ──▶ GroupSheet（複数グループ所属時のみ）
           │       ├─ 日付タップ ──▶ 下部ドロワー展開（同画面内）
           │       │                   └─ イベント行タップ ──▶ /events/[id]/edit（作成者のみ）
           │       └─ FAB（＋）──▶ /events/new?groupId=xxx
           │                           ├─ カラー編集ボタン ──▶ /color-labels
           │                           └─ 保存 ──▶ /
           │
           ├─▶ /todo（TODOリスト）
           │       │
           │       ├─ グループ名タップ ──▶ GroupSheet（複数グループ所属時のみ）
           │       ├─ リストタブ切替 ──▶ TodoListSelector（スクロール可能タブ）
           │       ├─ 「+ リスト追加」──▶ 新規リスト作成モーダル
           │       ├─ TODOアイテム行タップ（作成者のみ）──▶ 編集モーダル
           │       ├─ チェックボックスタップ ──▶ 完了トグル（担当者のみ・未設定時は全員可）
           │       └─ 下部入力バー ──▶ 新規アイテム追加（📅で期限・担当者入力展開）
           │
           ├─▶ /work（仕事用カレンダー）
           │       │
           │       ├─ グループ名タップ ──▶ GroupSheet（複数グループ所属時のみ）
           │       ├─ 日付タップ ──▶ シフト選択パネル展開（同画面内）
           │       │                   └─ パターン選択 ──▶ シフット割当・パネル閉じる
           │       └─ パターン管理ボタン ──▶ /work/patterns
           │
           └─▶ ⚙️ 設定ボタン（BottomNav）──▶ ドロップダウンメニュー
                   │
                   ├─ グループ ──▶ /groups-settings（グループ設定）
                   │                   ├─ グループ名タップ ──▶ GroupSheet（複数グループ所属時のみ）
                   │                   ├─ メンバー行タップ ──▶ MemberDetailModal（ボトムシート）
                   │                   │                           ├─ メモ編集・保存（自分のみ）──▶ PATCH /users/me
                   │                   │                           └─ 退会ボタン（権限あり時）──▶ 退会確認画面
                   │                   │                                                            └─ 退会確定 ──▶ DELETE /groups/:id/members/:userId
                   │                   ├─ グループを作成する ──▶ /groups/new
                   │                   └─ グループを解散する（オーナーのみ）──▶ 解散確認画面
                   │                                                              └─ 解散確定 ──▶ DELETE /groups/:id ──▶ /
                   │
                   ├─ プロフィール ──▶ /groups-settings/profile（準備中）
                   │
                   └─ アカウント ──▶ /groups-settings/account（準備中）
```

---

## 3. Next.js App Router ディレクトリ構成

```
apps/web/src/
├── app/
│   ├── (auth)/                      # 認証不要レイアウト
│   │   ├── login/
│   │   │   └── page.tsx             # 画面1: ログイン
│   │   └── register/
│   │       └── page.tsx             # 画面2: 新規登録
│   │
│   ├── (app)/                       # 認証必須レイアウト
│   │   ├── layout.tsx               # BottomNav + 認証ガード
│   │   ├── onboarding/
│   │   │   └── page.tsx             # 画面3: オンボーディング
│   │   ├── join/
│   │   │   └── [token]/
│   │   │       └── page.tsx         # 画面4: グループ参加
│   │   ├── page.tsx                 # 画面5: プライベートカレンダー
│   │   ├── todo/
│   │   │   └── page.tsx             # 画面5.5: TODOリスト
│   │   ├── events/
│   │   │   ├── new/
│   │   │   │   └── page.tsx         # 画面6: イベント作成
│   │   │   └── [id]/
│   │   │       └── edit/
│   │   │           └── page.tsx     # 画面7: イベント編集
│   │   ├── work/
│   │   │   ├── page.tsx             # 画面8: 仕事用カレンダー
│   │   │   └── patterns/
│   │   │       ├── page.tsx         # 画面9: シフトパターン一覧
│   │   │       ├── new/
│   │   │       │   └── page.tsx     # 画面10(新規): シフトパターン作成
│   │   │       └── [id]/
│   │   │           └── page.tsx     # 画面11: シフトパターン編集
│   │   ├── groups-settings/
│   │   │   ├── page.tsx             # 画面12: グループ設定
│   │   │   ├── profile/
│   │   │   │   └── page.tsx         # 画面14: プロフィール設定
│   │   │   └── account/
│   │   │       └── page.tsx         # 画面15: アカウント設定
│   │   └── color-labels/
│   │       └── page.tsx             # 画面16: カラー編集
│   │
│   └── layout.tsx                   # ルートレイアウト
│
└── components/
    ├── auth/                        # 認証関連
    │   ├── LoginForm.tsx
    │   └── RegisterForm.tsx
    ├── calendar/                    # プライベートカレンダー関連
    │   ├── CalendarGrid.tsx
    │   ├── CalendarHeader.tsx
    │   ├── DayCell.tsx
    │   ├── EventBadge.tsx
    │   ├── DayDrawer.tsx
    │   └── EventForm.tsx
    ├── todo/                        # TODOリスト関連
│   ├── TodoListSelector.tsx
│   ├── TodoItemList.tsx
│   └── TodoItemRow.tsx
    ├── work/                        # 仕事用カレンダー関連
    │   ├── WorkCalendarGrid.tsx
    │   ├── ShiftCell.tsx
    │   ├── ShiftSelectPanel.tsx
    │   ├── ShiftPatternList.tsx
    │   └── ShiftPatternForm.tsx
    ├── group/                       # グループ関連
    │   ├── OnboardingForm.tsx
    │   └── InviteLinkCard.tsx
    ├── settings/                    # 設定画面関連
    │   └── MemberDetailModal.tsx
    └── ui/                          # 共通UIコンポーネント
        ├── FAB.tsx
        ├── BottomNav.tsx
        ├── GroupSheet.tsx
        └── ColorPicker.tsx

> **注**: `color-labels/page.tsx` はカラーラベル管理ロジックをページ内にインライン実装（専用コンポーネントなし）
```

---

## 4. 各画面の詳細設計

---

### 画面1: ログイン `/login`

**目的**: メールアドレス＋パスワードでログイン

**レイアウト**

```
┌──────────────────────────┐
│      アプリロゴ            │
│                          │
│  [メールアドレス入力]       │
│  [パスワード入力]          │
│                          │
│  [ログインボタン]          │
│                          │
│  新規登録はこちら ──▶      │
└──────────────────────────┘
```

**コンポーネント構成**

```
page.tsx
└── LoginForm
    ├── <input type="email" />         メールアドレス
    ├── <input type="password" />      パスワード
    ├── <button> ログイン              送信ボタン
    └── <Link href="/register">        新規登録リンク
```

**状態・バリデーション**

- メール形式チェック・パスワード必須
- ログイン失敗時: エラーメッセージ表示
- ログイン成功: グループ所属 → `/` / 未所属 → `/onboarding`

---

### 画面2: 新規登録 `/register`

**目的**: アカウント作成

**レイアウト**

```
┌──────────────────────────┐
│      アプリロゴ            │
│                          │
│  [表示名入力]             │
│  [メールアドレス入力]       │
│  [パスワード入力]          │
│                          │
│  [登録ボタン]             │
│                          │
│  ログインはこちら ──▶      │
└──────────────────────────┘
```

**コンポーネント構成**

```
page.tsx
└── RegisterForm
    ├── <input type="text" />          表示名
    ├── <input type="email" />         メールアドレス
    ├── <input type="password" />      パスワード（8文字以上）
    ├── <button> 登録する              送信ボタン
    └── <Link href="/login">           ログインリンク
```

**状態・バリデーション**

- パスワード8文字以上
- メール重複時: 「このメールアドレスはすでに登録されています」
- 登録成功 → `/onboarding`

---

### 画面3: オンボーディング `/onboarding`

**目的**: グループ未所属ユーザーがグループを作成する

**レイアウト**

```
┌──────────────────────────┐
│  グループを作成してください  │
│                          │
│  [グループ名入力]          │
│  [作成ボタン]             │
│                          │
│  ─────── 作成後 ─────── │
│                          │
│  招待リンク               │
│  [https://...][コピー]    │
│                          │
│  [カレンダーへ進む]        │
└──────────────────────────┘
```

**コンポーネント構成**

```
page.tsx
├── OnboardingForm（グループ名入力・作成）
│   ├── <input type="text" />          グループ名
│   └── <button> 作成する
└── InviteLinkCard（作成後に表示）
    ├── <p> 招待リンク表示
    ├── <button> コピー               クリップボードコピー
    └── <Link href="/"> カレンダーへ進む
```

**状態管理**

- `step: 'create' | 'invite'` で表示切替
- 作成後: invite_tokenを含むURLを表示

---

### 画面4: グループ参加 `/join/[token]`

**目的**: 招待リンク経由でグループに参加

**レイアウト**

```
┌──────────────────────────┐
│  「〇〇家」に参加しますか？  │
│                          │
│  グループ名: 〇〇家        │
│  メンバー数: 3人           │
│                          │
│  [参加する]               │
│  [キャンセル]             │
└──────────────────────────┘
```

**コンポーネント構成**

```
page.tsx（サーバーコンポーネント: tokenからグループ情報取得）
└── JoinGroupCard
    ├── グループ名・メンバー数表示
    ├── <button> 参加する             POST /groups/join/:token
    └── <Link href="/onboarding"> キャンセル
```

**状態・バリデーション**

- 未ログイン: `/login?redirect=/join/[token]` にリダイレクト
- すでに同じグループに所属済み: 「すでにこのグループに所属しています」（409）
- 別グループ所属済みでも参加可能（複数グループ所属を許可）
- 無効トークン: 「招待リンクが無効です」
- 参加成功 → `/`

---

### 画面5: プライベートカレンダー `/`

**目的**: グループ共有イベントの閲覧・管理（メイン画面）

**レイアウト**

```
┌──────────────────────────┐
│ 〇〇家 ▼    [P][W] ‹ ›  │  ← CalendarHeader（グループ名タップで切替）
│ 6月                      │
│ [全員            ▼]      │  ← メンバー絞り込みセレクトボックス
│ 日 月 火 水 木 金 土      │
│─────────────────────────│
│  1  2  3  4  5  6  7   │
│        🟦片岡 🟪振り込み│  ← DayCell + EventBadge
│  8  9 10 11 12 13 14   │  ← CalendarGrid
│   🟦木村   🟦納期会     │
│ ...                      │
│──────── ドロワー ────────│  ← DayDrawer（日付タップ時）
│ 6月12日（金）             │
│ 18:00 ● 納期会           │
│                          │
│                      [＋]│  ← FAB
└──────────────────────────┘
│ 🏠 プライベート  💼 仕事  │  ← BottomNav
└──────────────────────────┘
```

**コンポーネント構成**

```
page.tsx
├── CalendarHeader
│   ├── グループ名（複数グループ所属時はタップで GroupSheet を開く）
│   ├── 年月表示（YYYY年M月）
│   ├── カレンダー切替トグル（[P][W]ボタン）   ← 表示中は `/`、もう一方は `/work`
│   ├── 月移動ボタン（‹ ›）
│   ├── メンバー絞り込みセレクトボックス        ← 「全員」＋グループメンバー一覧
│   └── 曜日ヘッダー（日〜土）
│
├── MonthSlider（css scroll-snap・前月/現在月/次月を縦並びで保持）
│   ├── CalendarGrid（前月・データなし）
│   ├── CalendarGrid（現在月・filteredEvents）
│   └── CalendarGrid（次月・データなし）
│       └── DayCell × 日数分
│           ├── 日付数字（今日はハイライト）
│           └── EventBadge × イベント数分（filteredEvents を表示）
│               └── タイトル・背景色
│
├── DayDrawer（selectedDate != null のとき表示）
│   ├── ドラッグハンドル
│   ├── 日付ヘッダー + 閉じるボタン
│   ├── EventRow × イベント数分（filteredEvents を表示）
│   │   ├── カラードット
│   │   ├── 時刻（終日 or HH:MM）
│   │   ├── タイトル
│   │   └── 編集ボタン（作成者のみ表示）──▶ /events/[id]/edit
│   └── 「予定なし」（イベントが0件のとき）
│
├── GroupSheet（showGroupSheet=true のとき表示・複数グループ時のみ）
│   └── グループ一覧（選択中はチェックマーク）
│
└── FAB（＋ボタン） ──▶ /events/new?groupId=<selectedGroupId>
```

**状態管理**

- `selectedDate: string | null` 選択日付
- `month: { year, month }` 表示月
- `events: Event[]` SWRでフェッチ（`groupId` クエリで選択グループに絞り込み）
- `filteredEvents: Event[]` `selectedMemberId` で `event.createdBy` を絞り込んだ結果（全員選択時は `events` と同一）
- `selectedGroupId: string | null` 選択グループID（localStorage で永続化）
- `selectedMemberId: string` 絞り込み対象メンバーID（`''` = 全員）
- `showGroupSheet: boolean` グループ切替シート表示状態
- `members` 選択グループの `members` 配列（`useGroups()` から取得）

**縦スクロール操作**

- `MonthSlider`（`components/ui/MonthSlider.tsx`）が前月・現在月・次月を縦に並べて保持
- CSS `scroll-snap-type: y mandatory` により各月にスナップ
- 下スクロール → 次月、上スクロール → 前月
- スナップ完了後に月状態を更新し、スクロール位置を中央（現在月）に自動リセット
- ‹ › ボタンでも月移動可能（MonthSlider が中央リセットを処理）
- 隣接月はカレンダー構造のみ表示（イベントデータなし）

**絞り込みロジック**

- 「全員」選択時（`selectedMemberId === ''`）: 全イベントを表示
- メンバー選択時: `event.createdBy === selectedMemberId` のイベントのみ表示
- グループ切り替え時は「全員」にリセット

---

### 画面5.5: TODOリスト `/todo`

**目的**: グループ内でTODOリストを共有管理する

**レイアウト**

```
┌──────────────────────────────┐
│ 〇〇家 ▼                     │  ← グループ名（複数グループ所属時はタップで切替）
│ 買うものリスト               │  ← 選択中リスト名（リストのアクセントカラー）
│──────────────────────────────│
│ ●買うもの  ●プライベート  ＋ │  ← TodoListSelector（横スクロール）
│──────────────────────────────│
│ ○ オリーブオイル             │  ← 未完了アイテム
│ ○ トマト缶                  │
│ ○ お風呂の洗剤               │
│──────────────────────────────│
│ ▶ 完了済み（2）              │  ← 折りたたみセクション
│──────────────────────────────│
│ 📅 [新しい項目            ] ▶│  ← 下部入力バー
└──────────────────────────────┘
│  🏠    ✅    💼    ⚙️        │  ← BottomNav
```

**コンポーネント構成**

```
page.tsx
├── ヘッダー
│   ├── グループ名ボタン（複数グループ所属時はタップ可・▼ 表示）──▶ GroupSheet
│   └── 選択中リスト名（リストのアクセントカラー）
│
├── TodoListSelector（リストタブ横スクロール）
│   ├── リストボタン × リスト数分（色ドット + 名称、選択中は枠付き）
│   │   └── × ボタン（作成者のみ）──▶ 削除確認 ──▶ DELETE /todo-lists/:id
│   └── 「+ リスト追加」──▶ 新規リスト作成モーダル
│       ├── <input> リスト名
│       ├── カラーパレット（8色）
│       └── 作成ボタン ──▶ POST /todo-lists { name, color, groupId }
│
├── TodoItemList（選択中リストのアイテム一覧）
│   ├── 未完了アイテム × 件数分（sort_order / createdAt 昇順）
│   │   └── TodoItemRow
│   │       ├── チェックボックス ──▶ PATCH /todo-items/:id { isCompleted: ! }
│   │       │       担当者が設定されている場合は担当者のみ有効（他はグレーアウト）
│   │       │       担当者未設定の場合はグループメンバー全員が有効
│   │       ├── タイトル（完了時は打ち消し線 + 薄く）
│   │       ├── メモ（任意）
│   │       ├── 🕐 期限日（青色・任意）
│   │       ├── 「作成: 〇〇」「担当: 〇〇」（小テキスト）
│   │       └── ✏️ 🗑️ ボタン（作成者のみ） ──▶ 編集モーダル / DELETE /todo-items/:id
│   │
│   └── 「▶ 完了済み（N）」折りたたみセクション
│       └── TodoItemRow × 完了済み件数分
│
├── 下部入力バー
│   ├── 📅 ボタン ──▶ 期限日・担当者入力エリア展開
│   │   ├── <input type="date"> 期限日
│   │   └── <select> 担当者（グループメンバー）
│   ├── <input> 新しい項目（テキスト入力）
│   └── ▶ 送信ボタン ──▶ POST /todo-items
│
└── GroupSheet（グループ選択シート）
    複数グループ所属時にグループ名タップで表示
```

**状態管理**

- `groups: Group[]` `useGroups()` SWRでフェッチ（`GET /groups`）
- `selectedGroupId: string | null` 選択中グループID（localStorage `nestday_selected_group_id` で永続化・カレンダー画面と共有）
- `showGroupSheet: boolean` グループ選択シートの表示状態
- `lists: TodoList[]` SWRでフェッチ（`GET /todo-lists?groupId=xxx`）
- `selectedListId: string | null` 選択中リストID（グループ変更時リセット、リスト取得後に先頭を自動選択）
- `items: TodoItem[]` SWRでフェッチ（`GET /todo-items?listId=xxx`）
- `showCompleted: boolean` 完了済みセクション開閉状態
- `showDueDate: boolean` 下部入力バーの期限日・担当者エリア展開状態
- `currentUserId: string` Supabase セッションから取得（作成者・担当者判定用）
- `members: Member[]` `groups` データの `selectedGroup.members` から取得（担当者選択用）

**権限まとめ**

| 操作 | 誰ができるか |
|------|------------|
| リスト作成 | グループメンバー全員 |
| リスト削除 | 作成者のみ |
| アイテム追加 | グループメンバー全員 |
| アイテム完了トグル | 担当者が設定されている場合は担当者のみ / 担当者未設定の場合はグループメンバー全員 |
| アイテム編集・削除 | 作成者のみ |

---

### 画面6: イベント作成 `/events/new?groupId=<id>`

**目的**: 選択中のグループにイベントを作成（LifeBearライクUI）

**レイアウト**

```
┌──────────────────────────┐
│ ×  予定作成        [保存]│
│──────────────────────────│
│ 予定名                   │
│──────────────────────────│
│ 金曜日  →  金曜日  [終日]│
│ 6月12日    6月12日  オン│
│──────────────────────────│
│ カラー          [カラー編集]│  ← 「カラー編集」ボタン → /color-labels
│ ● ● ● ● ● ● ● ● ● ●  │  ← プリセットカラー（EVENT_COLORS）
│                          │
│ マイカラー               │  ← カラーラベル登録済みの場合のみ表示
│ ●仕事 ●趣味 ...          │  ← 色丸＋名称付き
│──────────────────────────│
│    詳細を表示             │
│──────────────────────────│
│ （展開後）               │
│ 📍 場所                  │
│ 💬 メモ                  │
│──────────────────────────│
│ [        保存        ]   │  ← フォーム下部の保存ボタン
└──────────────────────────┘
```

**コンポーネント構成**

```
page.tsx
└── EventForm（新規モード）
    ├── ヘッダー
    │   ├── <button> × ──▶ /（キャンセル）
    │   └── <button> 保存（ヘッダー右端）
    ├── <input> タイトル（予定名・大きめフォント）
    ├── DateTimeSection
    │   ├── 開始日時ピッカー
    │   ├── 終了日時ピッカー
    │   └── Toggle（終日オン/オフ）
    ├── ColorSection
    │   ├── ラベル「カラー」+ <Link href="/color-labels"> カラー編集
    │   ├── ColorPicker（EVENT_COLORS プリセット）
    │   └── マイカラー欄（colorLabels.length > 0 のとき表示）
    │       └── カラーボタン × ラベル数分（色丸＋名称）
    ├── DetailSection（「詳細を表示」トグル）
    │   ├── <input> 場所（最大200文字）
    │   └── <textarea> メモ
    └── <button> 保存（フォーム下部・非readOnlyのみ）
```

**状態管理**

- `groupId: string | undefined` URLクエリパラメータ `?groupId=xxx` から取得。`GET /calendars?groupId=xxx` でそのグループのカレンダーのみ取得し使用する
- `colorLabels: ColorLabel[]` 初期化時に `GET /color-labels` で取得
- `isAllDay: boolean`（デフォルト: true）
- `showDetail: boolean`（「詳細を表示」開閉）
- 保存成功 → `/`（選択日付のドロワー展開状態で戻る）

---

### 画面7: イベント編集 `/events/[id]/edit`

**目的**: 既存イベントの編集・削除（作成者のみアクセス可能）

**レイアウト**: 画面6とほぼ同一。差分のみ記載

```
┌──────────────────────────┐
│ ×  予定編集        [保存]│
│  ...（画面6と同じ）...   │
│──────────────────────────│
│ [        保存        ]   │  ← フォーム下部の保存ボタン（作成者のみ）
│──────────────────────────│
│ [削除する]               │  ← 削除ボタン（赤・作成者のみ）
└──────────────────────────┘
```

**コンポーネント構成**

```
page.tsx（サーバーコンポーネント: イベント取得・権限チェック）
└── EventForm（編集モード）
    ├── （画面6と同じフォーム要素）
    ├── <button> 保存（フォーム下部・作成者のみ）
    └── <button> 削除する（赤・確認ダイアログあり・作成者のみ）
```

**状態・権限**

- 作成者以外がアクセス → `/` にリダイレクト
- 削除確認: `confirm()` or カスタムダイアログ
- 保存/削除成功 → `/`

---

### 画面8: 仕事用カレンダー `/work`

**目的**: グループ全員のシフトを月表示で確認・自分のシフトを設定

**レイアウト**

```
┌──────────────────────────┐
│ 2026        [P][W] ‹ ›  │  ← CalendarHeader（共通）
│ 6月                      │
│ [全員            ▼]      │  ← メンバー絞り込みセレクトボックス
│ 日 月 火 水 木 金 土      │
│─────────────────────────│
│  1   2   3   4   5  6   │
│  1  ①  3   6   1  ●   │  ← ShiftCell（filteredShifts を表示）
│─────────────── パネル ──│  ← ShiftSelectPanel（日付タップ時）
│ 6月12日                  │
│ 自分のシフト:             │
│ ┌────┐ ┌────┐ ┌────┐ │
│ │  1  │ │  3  │ │振休 │ │  ← シフットパターン選択グリッド
│ └────┘ └────┘ └────┘ │
│                      🔧  │  ← パターン管理ボタン（fixed・フッター上・右端）
│ 🏠 プライベート  💼 仕事  │  ← BottomNav
└──────────────────────────┘
```

**コンポーネント構成**

```
page.tsx
├── CalendarHeader（共通コンポーネント）
│   └── メンバー絞り込みセレクトボックス（「全員」＋グループメンバー一覧）
│
├── MonthSlider（css scroll-snap・前月/現在月/次月を縦並びで保持）
│   ├── WorkCalendarGrid（前月・データなし）
│   ├── WorkCalendarGrid（現在月・filteredShifts）
│   └── WorkCalendarGrid（次月・データなし）
│       └── ShiftCell × 日数分（filteredShifts を受け取る）
│           └── ShiftBadge × メンバー数分
│               ├── パターン名称（例: 1, 振休）
│               └── カラードット or 背景色
│
├── パターン管理ボタン（🔧・fixed・フッター上12px・右端16px） ──▶ /work/patterns
│
└── ShiftSelectPanel（selectedDate != null のとき表示）
    ├── 日付ヘッダー + 閉じるボタン
    ├── 「自分のシフト」セクション（絞り込みに関係なく常に自分のシフトを参照）
    │   └── ShiftPatternGrid
    │       └── ShiftPatternCard × パターン数分
    │           ├── パターン名称（色付き）
    │           ├── 時刻（start_time - end_time）
    │           └── タップ → PUT /shifts/:date
    └── 「メンバーのシフト」セクション
        └── MemberShiftRow × メンバー数分
            ├── アバター + 名前
            └── シフット名称（閲覧のみ）
```

**状態管理**

- `selectedDate: string | null`
- `month: { year, month }`
- `shifts: Shift[]` SWRでフェッチ（全メンバー分・フィルタ前の生データ）
- `filteredShifts: Shift[]` `selectedMemberId` で `shift.userId` を絞り込んだ結果（全員選択時は `shifts` と同一）
- `myPatterns: ShiftPattern[]` SWRでフェッチ
- `selectedMemberId: string` 絞り込み対象メンバーID（`''` = 全員）
- `members` 選択グループの `members` 配列（`useGroups()` から取得）

**縦スクロール操作**

- `MonthSlider` が前月・現在月・次月を縦に並べて保持（`scroll-snap-type: y mandatory`）
- 下スクロール → 次月、上スクロール → 前月
- スナップ完了後に月状態を更新し、スクロール位置を中央に自動リセット
- ‹ › ボタンでも月移動可能

**絞り込みロジック**

- 「全員」選択時（`selectedMemberId === ''`）: 全シフトを表示
- メンバー選択時: `shift.userId === selectedMemberId` のシフトのみ表示
- ShiftSelectPanel の自分シフト編集は `shifts`（フィルタ前）を参照するため、絞り込みの影響を受けない
- グループ切り替え時は「全員」にリセット

---

### 画面9: シフトパターン一覧 `/work/patterns`

**目的**: 自分のシフトパターンを管理（Image 3スタイル）

**レイアウト**

```
┌──────────────────────────┐
│ ‹  シフトパターン設定  ＋ │
│──────────────────────────│
│ 振休          休日       ›│
│ 1             7:05-15:50 ›│
│ ③             7:45-16:30 ›│
│ 3             7:45-16:30 ›│
│ ...                      │
│──────────────────────────│
│ [＋ 追加                ]│  ← 青背景の追加ボタン
└──────────────────────────┘
```

**コンポーネント構成**

```
page.tsx
└── ShiftPatternList
    ├── ヘッダー
    │   ├── <Link href="/work"> ‹ 戻る
    │   └── <Link href="/work/patterns/new"> ＋（ヘッダー右端）
    ├── パターン行 × パターン数分（sort_order昇順）
    │   ├── カラードット
    │   ├── パターン名称（色付き文字）
    │   ├── 時刻表示（HH:MM - HH:MM / 休日）
    │   └── › ──▶ /work/patterns/[id]
    └── <Link href="/work/patterns/new"> ＋ 追加（リスト下部・青ボタン）
```

---

### 画面10: シフトパターン新規作成 `/work/patterns/new` ／ 画面11: シフトパターン編集 `/work/patterns/[id]`

**目的**: シフトパターンの作成・編集（Image 2スタイル）

**レイアウト**

```
┌──────────────────────────┐
│ ‹  シフトパターン編集  [保存]│
│──────────────────────────│
│ 表示名称          [  1 ] │
│             [表示色変更]  │
│──────────────────────────│
│ 開始時間        7:05  ›  │
│ 終了時間       15:50  ›  │
│ 休憩時間        1:00  ›  │
│ 勤務時間        7:45     │  ← 自動計算（読み取り専用）
│ 休日                 ◯  │
│──────────────────────────│
│ [        保存        ]   │  ← フォーム下部の保存ボタン
│ （編集モードのみ）        │
│ [このパターンを削除]       │  ← 赤テキスト
└──────────────────────────┘
```

**コンポーネント構成**

```
page.tsx
└── ShiftPatternForm
    ├── ヘッダー
    │   ├── <Link href="/work/patterns"> ‹ 戻る
    │   └── <button> 保存（ヘッダー右端）
    ├── NameSection
    │   ├── <input> 表示名称
    │   └── ColorPicker（表示色変更）
    ├── TimeSection（is_day_off=false のとき表示）
    │   ├── TimePicker 開始時間
    │   ├── TimePicker 終了時間
    │   ├── TimePicker 休憩時間
    │   └── 勤務時間（自動計算: 終了-開始-休憩）
    ├── Toggle 休日フラグ
    ├── <button> 保存（フォーム下部）
    └── <button> 削除する（赤・編集モードのみ）
```

**状態管理**

- `isDayOff: boolean`（ONで時間セクション非表示）
- `workingTime: string`（開始・終了・休憩から自動計算）
- 保存/削除成功 → `/work/patterns`

---

### 画面12: グループ設定 `/groups-settings`

**目的**: グループ情報の確認・切替、招待リンクのコピー、ログアウト

**レイアウト**

```
┌──────────────────────────┐
│          設定             │
│──────────────────────────│
│ グループ                  │
│ ┌──────────────────────┐ │
│ │ 🅖 〇〇家     切替 ▼ │ │  ← 複数グループ所属時のみ「切替 ▼」表示
│ │   3人のメンバー       │ │
│ └──────────────────────┘ │
│──────────────────────────│
│ 招待リンク                │
│ [https://...][コピー]     │
│──────────────────────────│
│ メンバー（3人）           │
│ ┌──────────────────────┐ │
│ │ 🅐 田中太郎  オーナー ›│ │  ← 行タップ → メンバー詳細モーダル
│ │ 🅑 山田花子  自分    ›│ │
│ │ 🅒 鈴木一郎          ›│ │
│ └──────────────────────┘ │
│──────────────────────────│
│    ＋ グループを作成する   │
│──────────────────────────│
│       ログアウト           │
└──────────────────────────┘
```

**コンポーネント構成**

```
page.tsx
├── ヘッダー「設定」
├── グループセクション
│   └── グループ情報カード
│       └── <button>（複数グループ所属時のみ） ──▶ GroupSheet を開く
├── 招待リンクセクション
│   └── URLテキスト + コピーボタン
├── メンバーセクション
│   └── メンバー行 × 人数分（<button>）
│       ├── Avatar（メールアドレス頭文字・色）
│       ├── 表示名 + オーナーバッジ + 自分バッジ
│       ├── メールアドレス
│       └── › ──▶ MemberDetailModal を開く
├── <Link href="/groups/new"> グループを作成する
├── ログアウトボタン
├── GroupSheet（showGroupSheet=true のとき表示）
│   └── グループ一覧（選択中はチェックマーク）
└── MemberDetailModal（selectedMember != null のとき表示）
    └── 詳細画面 or 退会確認画面（step で切替）
```

**状態管理**

- `selectedGroupId: string | null` localStorage の `nestday_selected_group_id` と同期（カレンダー画面と共有）
- `showGroupSheet: boolean` グループ切替シート表示状態
- `groups: ApiGroupSummary[]` `GET /groups` の結果（`useGroups()` フック）
- `selectedMember: Member | null` 詳細を表示するメンバー（null で非表示）

**注意事項**

- グループが1つのみの場合はグループ名がタップ不可（「切替 ▼」非表示）
- `selectedGroupId` はカレンダー画面（`/`、`/work`）と localStorage を通じて共有される

---

### モーダル: メンバー詳細（MemberDetailModal）

**目的**: メンバーの詳細確認・メモ編集・退会操作

**遷移元**: 設定画面のメンバー行タップ

#### 詳細画面（step: 'detail'）

```
┌──────────────────────────┐
│ ─────                    │  ← ドラッグハンドル
│ メンバー詳細          ✕  │
│──────────────────────────│
│         🅐               │  ← 大きなアバター（80px）
│      田中太郎            │
│   [オーナー] [自分]       │  ← 該当する場合のみ表示
│──────────────────────────│
│ メールアドレス  foo@...   │
│ 表示名         田中太郎  │  ← name が設定されている場合のみ
│──────────────────────────│
│ MEMO                     │
│ ┌──────────────────────┐ │
│ │ 自分の場合: 編集可能  │ │  ← textarea（自分のみ）
│ │ 他人の場合: 読み取り専用│ │  ← div（他メンバー）
│ └──────────────────────┘ │
│          [文字数] [保存]  │  ← 自分のみ表示
│──────────────────────────│
│  グループから退会する/    │  ← 退会権限がある場合のみ
│  グループから退会させる   │    本人→「退会する」
│                          │    オーナーが他メンバー→「退会させる」
└──────────────────────────┘
```

#### 退会確認画面（step: 'confirm'）

```
┌──────────────────────────┐
│ ─────                    │
│ ← 戻る  退会確認     ✕  │
│──────────────────────────│
│          ⚠️               │  ← 警告アイコン
│                          │
│  田中太郎 をグループから  │  ← 本人の場合「グループから退会しますか？」
│  退会させますか？         │    オーナー操作の場合「〇〇 をグループから退会させますか？」
│                          │
│  退会後はカレンダーに     │
│  アクセスできなくなります  │
│  招待リンクから再参加可能  │
│──────────────────────────│
│ ┌──────────────────────┐ │  ← 対象メンバー情報
│ │ 🅐 田中太郎  foo@... │ │
│ └──────────────────────┘ │
│──────────────────────────│
│  [退会確定]               │  ← 赤ボタン → confirm() → API
│  [キャンセル]             │
└──────────────────────────┘
```

**コンポーネント構成**

```
MemberDetailModal
├── step: 'detail'
│   ├── AvatarLarge（80px アバター）
│   ├── 表示名 + バッジ（オーナー / 自分）
│   ├── InfoRow × 2（メールアドレス / 表示名）
│   ├── メモ欄
│   │   ├── <textarea>（isMe=true のとき編集可・保存ボタン）  PATCH /users/me
│   │   └── <div>（isMe=false のとき読み取り専用）
│   └── 退会ボタン（canRemove=true のとき表示） ──▶ step='confirm'
│
└── step: 'confirm'
    ├── 警告アイコン
    ├── 確認メッセージ（本人 / オーナー操作で文言切替）
    ├── 対象メンバーカード
    ├── 退会確定ボタン ──▶ confirm() ──▶ DELETE /groups/:groupId/members/:userId
    └── キャンセルボタン ──▶ step='detail'
```

**退会権限ロジック**

| 閲覧者 | 対象メンバー | 退会ボタン表示 | ボタン文言 |
|--------|-------------|--------------|-----------|
| オーナー | 自分（オーナー） | ✗ 非表示 | - |
| オーナー | 他のメンバー | ✅ 表示 | グループから退会させる |
| 一般メンバー | 自分 | ✅ 表示 | グループから退会する |
| 一般メンバー | 他のメンバー | ✗ 非表示 | - |

**プロパティ**

| プロパティ | 型 | 説明 |
|---|---|---|
| `isOpen` | boolean | 表示状態 |
| `member` | Member \| null | 表示対象メンバー |
| `isOwner` | boolean | 対象メンバーがグループオーナーか |
| `isMe` | boolean | 対象メンバーが自分か |
| `isCurrentUserOwner` | boolean | 現在のログインユーザーがグループオーナーか |
| `groupId` | string \| null | グループID（退会API用） |
| `onClose` | () => void | 閉じるコールバック |
| `onMemoSaved` | (memo: string) => void? | メモ保存後コールバック |
| `onMemberRemoved` | () => void? | 退会処理後コールバック |

---

### 画面16: カラー編集 `/color-labels`

**目的**: イベントに使用するカラーラベル（色と名称のペア）を管理する

**遷移元**: イベント作成・編集画面のカラーセクション「カラー編集」ボタン

**レイアウト**

```
┌──────────────────────────┐
│ ‹  カラー編集             │
│──────────────────────────│
│ デフォルトカラー          │  ← EVENT_COLORS の10色
│ 名称を入力して保存すると、 │
│ 予定作成時に名前付き表示  │
│                          │
│ ● [仕事        ] [保存][削除]│  ← 名称あり → 削除ボタン表示
│ ● [            ] [保存]  │  ← 名称なし → 削除ボタン非表示
│ ● [プライベート] [保存][削除]│
│  ...（10色分）            │
│──────────────────────────│
│ カスタムカラーを追加      │
│ カラー: ● ● ● ● ...     │  ← カラースウォッチ
│ 名称: [______________]   │
│ [追加]                   │
│──────────────────────────│
│ カスタムカラー一覧        │  ← 非デフォルト色のラベルのみ表示
│ ● 会議    [編集][削除]   │
│ ● 習い事  [編集][削除]   │
│   （編集展開時）          │
│   カラー: ● ● ...       │
│   名称: [______________] │
│   [キャンセル][保存]     │
│   [削除する]             │  ← 編集フォーム内の削除ボタン
└──────────────────────────┘
```

**コンポーネント構成**

```
app/(app)/color-labels/page.tsx（インライン実装）
├── ヘッダー
│   └── <button> ‹ 戻る（router.back()）
├── デフォルトカラーセクション
│   └── 各色の行（EVENT_COLORS × 10）
│       ├── 色丸（24px）
│       ├── <input> 名称（最大30文字・既存ラベルがあればプリフィル）
│       ├── <button> 保存（作成 or 更新）
│       └── <button> 削除（名称が保存済みの場合のみ表示）
├── カスタムカラー追加フォーム
│   ├── ColorSwatches（10色パレット）
│   ├── <input> 名称（最大30文字）
│   └── <button> 追加
└── カスタムカラー一覧（非EVENT_COLORS のラベルのみ）
    └── 各ラベルの行
        ├── 通常表示: 色丸＋名称＋編集ボタン＋削除ボタン
        └── 編集展開時: ColorSwatches＋名称入力＋キャンセル＋保存＋削除する
```

**状態管理**

- `labels: ColorLabel[]` `GET /color-labels` で取得（全ラベル）
- `defaultNames: Record<string, string>` デフォルトカラーの名称入力値（hex → name）
- `defaultSaving: string | null` 保存中のデフォルトカラーのhex
- `addName / addColor` 追加フォームの入力値
- `editId / editName / editColor` カスタムカラーのインライン編集状態

**カラーラベル保存ロジック**

| 操作 | 条件 | API呼び出し |
|------|------|------------|
| デフォルト保存 | 既存ラベルなし・名称あり | `POST /color-labels` |
| デフォルト保存 | 既存ラベルあり・名称あり | `PATCH /color-labels/:id` |
| デフォルト削除 | 削除ボタン押下 | `DELETE /color-labels/:id` → 入力フィールドクリア |
| カスタム追加 | 名称あり | `POST /color-labels` |
| カスタム更新 | 編集フォーム保存 | `PATCH /color-labels/:id` |
| カスタム削除 | 表示行 or 編集フォームの削除ボタン | `DELETE /color-labels/:id` |

**表示分類ロジック**

- `defaultHexSet` = `EVENT_COLORS` を大文字で Set 化
- `customLabels` = `labels.filter(l => !defaultHexSet.has(l.color.toUpperCase()))`
- デフォルトカラーの名称は `labels.find(l => l.color.toUpperCase() === color.toUpperCase())` で照合

---

## 5. 共通コンポーネント詳細

### BottomNav

画面5・5.5・8・9・10・11・12・14・15で常時表示。画面6・7（イベントフォーム）は全画面表示のため非表示。現在地をハイライト。

```
┌────────────────────────────────────────┐
│  🏠       ✅       💼          ⚙️      │
│ プライベート TODO  仕事用     設定      │
└────────────────────────────────────────┘
```

| タブ         | アイコン | 動作                                     |
| ------------ | -------- | ---------------------------------------- |
| プライベート | 🏠       | `/` へ遷移                               |
| TODO         | ✅       | `/todo` へ遷移                           |
| 仕事用       | 💼       | `/work` へ遷移                           |
| 設定         | ⚙️       | ドロップダウンメニューを上方向に展開     |

**設定ドロップダウン**

タップで BottomNav の上にドロップダウンメニューが展開する。

```
┌──────────────────┐
│ 👥 グループ      │  ──▶ /groups-settings
│ 👤 プロフィール  │  ──▶ /groups-settings/profile
│ 🔐 アカウント   │  ──▶ /groups-settings/account
└──────────────────┘
          ↑
 ┌──────────────────────────────┐
 │  🏠          💼         ⚙️   │
 └──────────────────────────────┘
```

- 現在表示中のページに対応するメニュー項目はハイライト表示
- メニュー外タップ または ページ遷移で自動クローズ

### CalendarHeader

画面5・8で共通使用。

| 要素                       | 説明                              |
| -------------------------- | --------------------------------- |
| グループ名                 | 選択中グループ名。複数グループ所属時はタップ可能（`▼` 表示）。タップで GroupSheet を開く |
| 年表示                     | YYYY（小さめ）                    |
| 月表示                     | M月（大きめ・太字）               |
| カレンダー切替             | `[P]`=プライベート / `[W]`=仕事用 |
| 月移動                     | ‹（前月）› （次月）               |
| メンバー絞り込みセレクト   | 「全員」＋グループメンバー一覧。本人には「（自分）」を付加 |
| 曜日ヘッダー               | 日〜土（日=赤・土=青）            |

| プロパティ          | 型                              | 説明                                       |
| ------------------- | ------------------------------- | ------------------------------------------ |
| `groupName`         | string?                         | 選択中グループ名                           |
| `hasMultipleGroups` | boolean?                        | 複数グループ所属時 `true`。グループ名をタップ可能にする |
| `onGroupTap`        | () => void?                     | グループ名タップ時コールバック             |
| `members`           | `{ id, name?, email }[]`?       | セレクトボックスに表示するメンバー一覧     |
| `currentUserId`     | string?                         | 本人判定用。一致するメンバーに「（自分）」を表示 |
| `selectedMemberId`  | string?                         | 選択中メンバーID（`''` = 全員）            |
| `onMemberChange`    | (memberId: string) => void?     | メンバー選択変更コールバック               |

### GroupSheet

グループ切替用のボトムシート。画面5・8・12で使用。

```
┌──────────────────────────┐
│ ─────                    │  ← ドラッグハンドル
│ グループを選択       ✕   │
│──────────────────────────│
│ [🅖] 〇〇家              │  ← 選択中（枠付き・青背景）✓
│ [🅗] 仕事用グループ      │
│ ＋ 新しいグループを作成   │  ← /groups/new へ遷移
└──────────────────────────┘
```

| プロパティ        | 型                       | 説明                            |
| ----------------- | ------------------------ | ------------------------------- |
| `isOpen`          | boolean                  | 表示状態                        |
| `groups`          | ApiGroupSummary[]        | グループ一覧                    |
| `selectedGroupId` | string \| null           | 選択中グループID                |
| `onSelect`        | (groupId: string) => void | グループ選択コールバック        |
| `onClose`         | () => void               | 閉じるコールバック              |

### Drawer / ShiftSelectPanel

タップで下から展開するボトムシート。

| プロパティ | 型         | 説明                   |
| ---------- | ---------- | ---------------------- |
| `isOpen`   | boolean    | 表示状態               |
| `onClose`  | () => void | 閉じるコールバック     |
| `date`     | string     | 対象日付（YYYY-MM-DD） |

### ColorPicker

イベント色・シフトパターン色の選択UI。

```
● ● ● ● ● ● ● ● ● ●
赤 橙 黄 緑 青緑 青 紫 紫 灰 黒
```

| プロパティ | 型                      | 説明                                       |
| ---------- | ----------------------- | ------------------------------------------ |
| `value`    | string                  | 選択中のHEXカラー                          |
| `onChange` | (color: string) => void | 選択コールバック                           |
| `colors`   | string[]                | 選択肢（`SHIFT_COLORS` or `EVENT_COLORS`） |

---

### 画面14: プロフィール設定 `/groups-settings/profile`

**目的**: ユーザーのプロフィール情報を確認・編集する（画面設計予定）

**現在の状態**: 準備中（「この画面は準備中です」を表示）

---

### 画面15: アカウント設定 `/groups-settings/account`

**目的**: セッション管理・アカウント削除を行う

#### レイアウト（step: 'main'）

```
┌──────────────────────────┐
│ ←  アカウント            │
├──────────────────────────┤
│ セッション               │
│ ┌────────────────────┐   │
│ │     ログアウト      │   │  ← 青文字ボタン
│ └────────────────────┘   │
│                          │
│ 危険な操作               │
│ ┌────────────────────┐   │
│ │  アカウント削除    │   │  ← 赤文字ボタン → step='confirm'
│ └────────────────────┘   │
│ アカウントを削除します。  │
│ この操作は取り消せません。│
└──────────────────────────┘
```

#### 確認画面（step: 'confirm'）

```
┌──────────────────────────┐
│ ← アカウント削除の確認  │
├──────────────────────────┤
│          ⚠️              │
│                          │
│ 本当にアカウントを       │
│ 削除しますか？           │
│                          │
│ アカウントを削除すると   │
│ 以下のデータが削除されます│
│ ・アカウント情報         │
│  （名前・メール・メモ）  │
│ ・全グループのメンバー   │
│   シップ                 │
│                          │
│ ※この操作は取り消せません│
│ ※オーナーは削除不可      │
│                          │
│ [  削除を確定  ]         │  ← 赤ボタン → DELETE /users/me
│ [  キャンセル  ]         │  ← step='main' に戻る
└──────────────────────────┘
```

#### フロー

```
アカウント削除ボタン
  └── step='confirm'
        ├── 削除を確定 ──▶ DELETE /users/me ──▶ signOut ──▶ /login
        └── キャンセル ──▶ step='main'
```

---

## 6. 認証ガード設計

Next.js Middleware で制御。

```
middleware.ts
├── 未認証 + 保護ルート → /login にリダイレクト
├── 認証済み + /login, /register → / にリダイレクト
└── 認証済み + グループ未所属 + 保護ルート（/join 以外）→ /onboarding にリダイレクト
```

| ルート                                        | 未認証     | 認証済み・グループ未所属 | 認証済み・所属済み |
| --------------------------------------------- | ---------- | ------------------------ | ------------------ |
| `/login`, `/register`                         | ✅ 表示    | → `/`                    | → `/`              |
| `/onboarding`                                 | → `/login` | ✅ 表示                  | → `/`              |
| `/join/[token]`                               | → `/login` | ✅ 表示                  | ✅ 表示（複数グループ参加可）|
| `/`, `/todo`, `/work`, `/events/*`, `/work/patterns/*` | → `/login` | → `/onboarding`          | ✅ 表示            |
| `/groups-settings`, `/groups-settings/*`            | → `/login` | ✅ 表示                  | ✅ 表示            |
